from django.contrib import admin

# Register your models here.
# from .forms import PipeForm, RecordForm
# from .models import Pipe, Record


# class PipeAdmin(admin.ModelAdmin):  # customize how the admin work
#     list_display = ['part_id', 'part_year', 'diameter', 'length', 'material']
#     form = PipeForm    # define which attribute could be edited in admin
#
#
# class RecordAdmin(admin.ModelAdmin):  # customize how the admin work
#     list_display = ['pipe', 'failure_year', 'failure_type']
#     form = RecordForm
#
# admin.site.register(Pipe, PipeAdmin)
# admin.site.register(Record, RecordAdmin)